// we can use ternary operator as a part of if condition 
let age = 16;
age = (age >= 18) ? "You can vote" : "You can not vote";
console.log(age);

// first value will print if the condition is true 
let myAge = 13;
myAge = (myAge <= 18) ? "you can not vote" : "You can vote";
// console.log(myAge);
